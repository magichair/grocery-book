# Dependencies
node_modules
.pnpm-store

# Build outputs
.next
dist
.turbo

# Environment
.env
.env.local
.env.*.local
# Allow the packages/db dev placeholder so `prisma validate` works on a fresh clone
!packages/db/.env

# Prisma
packages/db/src/generated

# Claude Code — local-only files (skills are installed per-developer, not committed)
.claude/settings.local.json
.claude/skills/

# Misc
.DS_Store
*.log
.claude/worktrees/
